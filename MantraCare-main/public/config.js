/**
 * Runtime configuration for API URLs.
 * This avoids the localhost problem in production builds.
 */

window._env_ = {
  // Production backend URL
  API_BASE_URL: "https://mantra-comprehensive-mental-health.onrender.com",

  // Optional: other runtime variables can go here
  // e.g. feature flags, analytics, etc.
};
