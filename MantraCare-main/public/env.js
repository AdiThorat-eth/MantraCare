/**
 * ✅ Runtime environment config
 * This file lets your deployed app know which backend to talk to.
 * Works for both local dev and production.
 */

window._env_ = {
  // 👇 Production backend URL (Render)
  API_BASE_URL: "https://mantra-comprehensive-mental-health.onrender.com",

  // You can add other runtime keys later if needed
  // FEATURE_X_ENABLED: true,
};
